{
  "name": "oneclick-chatgpt-plugin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Turn a One Click website brief into a structured Lovable build from ChatGPT.",
  "scripts": {
    "lint": "node scripts/check-source.mjs",
    "test": "node --test",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "check": "npm run lint && npm test && npm run validate:plugin",
    "types": "npx --yes wrangler@4.33.1 types",
    "dev": "npx --yes wrangler@4.33.1 dev",
    "deploy": "npx --yes wrangler@4.33.1 deploy",
    "deploy:dry": "npx --yes wrangler@4.33.1 deploy --dry-run",
    "smoke:production": "node scripts/smoke-production.mjs"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}

